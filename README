This files contains instructions on usage of the DuckChat system and the build
process to compile from src.

Follow the steps immidiately below to build the DuckChat server and client:
1. create a 'bin' directory in the project directory
2. run the command 'make' with the target 'ix'
